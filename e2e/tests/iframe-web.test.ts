describe('Iframe webpage', function () {
  /**
   * TODO: impl html can be visited
   * html : nexus/e2e/mock-page-with-disallowed-iframe/index.html
   * show : `chrome-extension://${extensionId}/popup.html`
   * expected
   * 1. iframe webpage is blocked
   */
  it.todo('should blocked when the iframe webpage link is nexus');

  /**
   * TODO: impl html can be visited
   * html : nexus/e2e/mock-page-with-iframe/index.html
   * 1. iframe webpage send wallet_enable request
   * expected
   * 1. nexus think iframe webpage is current webpage
   */
  it.todo('the iframe webpage has the same permissions as the current webpage');
});
